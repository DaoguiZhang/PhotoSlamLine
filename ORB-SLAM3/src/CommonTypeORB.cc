#pragma once

#include "CommonTypeORB.h"  //added for LineExtractor
#include <iostream>
#include <vector>

namespace ORB_SLAM3
{
    void PrintErrorInfo(int nErr)
    {
        std::cerr << "print error info(to do next)..." << std::endl;
    }
}